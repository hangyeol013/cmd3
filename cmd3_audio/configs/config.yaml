defaults:
  - model: vggish
  - compnode: light_memory_1_16
  - dataset: cmd3

root_dir: ${hydra:runtime.cwd}
xp_name: cmd3_0303_all_last
# xp_name: test
checkpoint: last.ckpt
checkpoint: cmd3_0303_all-epoch=2-val_loss=0.00.ckpt
full_checkpoint: ${hydra:runtime.cwd}/outputs/audio/2022-03-03/audio_all/checkpoints/${checkpoint}
result_dir: ${hydra:runtime.cwd}/results/audio/${xp_name}
feature_dir: ${root_dir}/custom_features/audio/${xp_name}
num_epochs: 100